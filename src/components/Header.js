import React from 'react';

export default function Header(){
    const cssHeader = {
        background : "",
        height : "5%",
        width : "100%",
    };
    return (
        <div style={cssHeader}>

        </div>
    );
};